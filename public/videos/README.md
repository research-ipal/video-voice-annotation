# Video Storage

This app does not store videos in the GitHub repository.

The selected videos are loaded from Hugging Face URLs defined in `src/videoManifest.js`.
Keep this folder empty unless you deliberately move to a separate static storage or CDN
strategy. Do not commit large video files directly to GitHub.
